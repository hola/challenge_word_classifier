Generation:
1. Remove words with 's
2. Soundex + Bloom to different files
3. merge files wit cat words.sdx words.blum > words.out

Checking:
1. Check if bloom fails
2. If bloom is ok, then check if soundex fails

